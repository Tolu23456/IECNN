[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "IECNN - Iterative Emergent Convergent Neural Network"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.24.0",
    "pillow>=10.0.0",
    "regex>=2023.0.0",
    "google-generativeai>=0.5.0",
]
